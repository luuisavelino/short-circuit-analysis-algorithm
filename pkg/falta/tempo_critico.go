package falta

import (
	//"math"
)

const frequencia_da_rede = 60


func tempo_critico(potencia_max_pos, potencia_media float64) {

	//H := 5.0
	//Ws := 2 * math.Pi * frequencia_da_rede
	//var Xd, V_1, V_2 float64
	//complex128 

	//angulo_tensao_barra_gerador := (potencia_media * Xd)/(V_1 * V_2)
	//corrente_saida_gerador := (V_1 - V_2) / Xd
	//tensao_gerador := V_1 + j * Xd * corrente_saida_gerador

	//potencia_media_pre_falta_barra_1 := tensao_gerador * V_1 / Xd_gerador
	//potencia_media_pre_falta_barra_2 := tensao_gerador * V_2 / Xd_gerador

	//potencia_media_pos_falta_barra_2 := tensao_gerador * V_2 / Xeq
	//delta_1 := math.Asin(potencia_media/potencia_media_pos_falta_barra_2)
	//detla_max := math.Pi - delta_1


	//delta_c := math.Acos((potencia_media * (detla_max - delta_0) + potencia_max_pos * math.Cos(detla_max)) / potencia_max_pos)
	//tempo := math.Sqrt((4 * H * (delta_c - delta_0)) / (Ws * potencia_media))

	//return delta_c, tempo
}